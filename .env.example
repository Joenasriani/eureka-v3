# Optional AI route. Leave OPENAI_API_KEY empty to use deterministic local mode.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6-terra

# Local-only by default. Change only behind appropriate access controls.
EUREKA_HOST=127.0.0.1
PORT=8787
